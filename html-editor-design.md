# 页内：沉浸式 HTML 文字编辑设计 v0.3

打开单个 HTML，默认完整展示页面。预览状态下，编辑器只在右上角悬浮一个「编辑」按钮。进入编辑后双击文字直接修改，保存为新版本，并支持操作撤销与历史版本回退。

## 预览状态

- 原 HTML 使用整个视窗，按原文件规则渲染和滚动。
- 编辑器不增加页头、页脚、侧栏、画布背景、外框、缩放或留白。
- 唯一的编辑器控件是右上角悬浮「编辑」按钮，不占文档布局空间。
- 无悬停边框、选中状态、输入光标、状态提示条或修改标记。
- 初次打开和刷新后进入预览。由编辑切换预览时展示当前草稿，但不自动保存。

## 编辑状态

点击「编辑」，右上角按钮原位展开为紧凑悬浮工具条：

`版本 / 未保存状态 | 撤销 · 重做 | 版本记录 | 保存新版本 | 预览 | 更多`

页面宽高与滚动位置保持不变，没有右边栏。更多菜单收纳「打开 HTML」「导出当前 HTML」等低频操作。编辑态底部可显示一条轻量操作提示；返回预览立即隐藏。

1. 双击一处文字，原位出现光标，可直接输入。
2. 使用原字体、字号、颜色等全部样式；粘贴只保留纯文本。
3. 点击空白处或按 Enter 完成本次输入；按 Esc 取消当前这一次输入。
4. 一次输入会话合并为一个撤销步骤；支持撤销和重做。
5. 切换预览时提交当前输入为草稿，移除全部编辑标记和工具条，仅保留「编辑」。

编辑期间拦截页面导航和表单提交，避免点击文字跳离页面。预览不增加这些编辑拦截。对于本稿静态示例，页面按钮本身未配置业务动作。

## 保存与回退

**保存新版本**：有改动时可用。完成当前输入，将页面内容保存为一个不可变版本，成功后显示「vN · 已保存」。没有改动时禁用，避免重复版本。版本以文本内容与必要文档数据持久保存，不把运行后的 DOM 当作文件源码。

**版本记录**：使用居中弹窗，按新到旧显示版本编号、保存时间、简短说明和内容摘要。可导出每个版本的 HTML，或回退到某个版本。原始内容始终为 v0。

**回退**：将目标版本内容恢复为一个新的最新版本；保留被回退后的所有历史。若有未保存草稿，先自动保存一份「回退前备份」，再保存回退结果。两者作为一次存储操作，失败则不改当前内容。版本回退后仍可撤销恢复刚才的文字内容。

例如：v0 → v1 → v2，回退到 v0 后新增 v3，其内容与 v0 一致，v1、v2 继续保留。

**导出 HTML**：与保存版本分开。保存版本负责历史记录，导出负责生成独立 HTML 文件。导出文件不带编辑按钮、工具条或编辑器脚本。命名示例：`slow-living-v3.html`。未保存内容可导出为 draft，但导出草稿不会清除未保存标记。

## 文件流程与边界

初次没有文件时显示单个打开 / 拖入入口。已有页面时，「打开 HTML」收纳在编辑态更多菜单。一次只打开一个文件。切换文件或关闭时，若有未保存草稿，提供保存、放弃、取消；正式版应提供崩溃恢复草稿。

仅修改静态可见文本内容。不修改样式、标签结构、链接目标、图片或脚本。嵌套加粗或链接里的文字按文本节点映射，避免改字时覆盖内部标签。文字变长引起的自然换行按原 CSS 发生，不自动缩字或改容器尺寸。

原文件中的 `<br>` 和其他结构保留；Enter 用于结束输入，不插入新 HTML 元素。脚本生成文字、图片内文字和 CSS 伪元素不属于首版编辑范围。

## 实现原则

- 文档在独立 iframe 中铺满视窗，编辑器控件由宿主页面浮层渲染，避免 CSS 相互影响。
- 编辑状态注入的标记和辅助样式在预览时完全移除；两种状态不改变 iframe 尺寸。
- 编辑时禁用原页面脚本；静态预览保留原始布局。需要脚本才能完成的渲染不承诺完全复原，后续若提供脚本预览应在独立受限实例运行，运行时 DOM 不直接回写。
- 基于原文件源码与文本位置映射保存最小文本补丁，转义特殊字符，保留 DOCTYPE、标签、属性、注释、样式、脚本与字符编码规则。
- 正式版每个文档分别建立持久版本记录，不能仅以文件名区分文档；可采用 IndexedDB 保存文档与版本。存储失败必须保持未保存状态并允许导出。
- 同目录资源需解决本地权限或桌面文件访问；资源缺失应明确提示，不能静默声称复原了全部 HTML。

## 本次交互设计稿

示例页面支持双击改字、Esc 取消、撤销重做、保存新版本、居中版本记录、回退前自动备份、历史回退、HTML 导出。版本存储在当前浏览器的 localStorage，刷新后恢复最新保存版本；清除浏览器数据会丢失记录，不代表已写回用户文件。

任意 HTML 导入、复杂文本节点映射和正式版持久存储尚未实现。「打开 HTML」展示流程说明。原型的保存与回退使用真实的示例版本记录。

## 验收重点

- 预览仅显示原 HTML 和右上角编辑按钮，无其他编辑器可见元素。
- 编辑与预览切换不改变页面布局宽度；双击直接修改，不需要面板。
- 保存后刷新仍可恢复版本，版本导出没有编辑器内容。
- 撤销恢复一次输入；Esc 仅取消当前输入；回退保留已有版本。
- 草稿回退前先备份，存储失败不丢失草稿。
