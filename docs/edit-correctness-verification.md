# 编辑正确性修复验收

日期：2026-09-26。PR 集成基线：`c42a4c0b8b2ab906645035e516935c6a389f6f4e`。原始 review 基线为 `9af26e59308e94f7dc41a30839d152dc066f1a8d`。本次按用户要求提交独立修复 PR，未发布、未改版本或依赖。

## 实施与独立审核

用户明确要求子 Agent 修复、主 Agent 审核。两名 worker 分别在独立 worktree 实施输入事务和文本上下文，主 Agent 逐文件审核完整补丁后按 A → B 串行集成；每次集成均通过类型检查。所有源文件遵守[所有权计划](edit-correctness-plan.md)，没有跨 worker 文件编辑。

主 Agent 核查了失败重试/取消、并发 finish/begin、IME 最后一次输入、空 Text 节点与命中几何、隐藏页面、元素及祖先变换、原始补丁区间与换行补偿。审核补充发现正文中的 `noframes` 仍可编辑，退回原 owner 加入只读集合和三组回归夹具后再集成。

修复范围与取舍见 [ADR-014](ADR-014-edit-transaction-fidelity.md) 和[契约](edit-correctness-contract.md)。四项原始缺陷均已有实现及自动回归覆盖；同时修复了原始单独 CR 与补丁 LF 合并、预览首 LF 再次被吞掉的边界。

接入最新 main 后，保留已合并的职责拆分、隔离动态报告、全屏焦点和 Linux 分发。主 Agent 单线完成适配，Worker A 只读复审并指出动态快照序列化再次吞掉首 LF 的问题；修复后复审通过。测试实际执行生产 liveBridge capture handler，覆盖 live HTML → DOM → 克隆序列化 → 净化 → 编辑 DOM；反向移除补偿时，两项 LF 回归失败，证明测试覆盖了该缺陷。

## 自动验证

环境由 mise 提供：Node 24.12.0、npm 11.6.2、Rust/Cargo 1.92.0；执行前已检查实际路径与版本。

| 检查                                        | 结果       | 证据与范围                                                                                     |
| ------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------- |
| `mise run check`                            | 通过       | 版本/身份、typecheck、139 项 Node 测试、21 项 Rust 测试                                        |
| `cargo fmt --check`                         | 通过       | 全部 Rust 源码                                                                                 |
| `cargo clippy --all-targets -- -D warnings` | 通过       | 无 warning                                                                                     |
| `npm run build`                             | 通过       | 由 Tauri 构建调用，包含类型检查及生产前端资源                                                   |
| macOS app 构建                              | 通过       | `mise run build`，包含最新 main 适配及快照修复；不等于签名、公证或发布包验收                   |

共同夹具有 26 组，Node 验证真实解析器清单和 HTML 重解析，Rust 以同一夹具验证实际 Session 导出。原始 9af26e5 基线阶段另通过 13 组独立交叉探针：实际 parseDocument → 实际 Rust Session → 导出 → HTML 重解析，验证补丁外字节、无修改及 undo/redo；最新集成阶段通过实际 macOS 导出完成跨层复验。

日志和独立探针位于忽略目录 `artifacts/fix-2026-09-26/`，不会进入提交或发布包。Node 控制器测试使用明确的 DOM 替身，IPC 拒绝由测试注入；它们不代表真实系统 WebView 通过。

## PR 最终构建的原生桌面验收

应用路径：`src-tauri/target/release/bundle/macos/PageIn.app`，已通过进程路径确认运行的是该产物。可执行文件 SHA-256：`e5be68b47c7bcd048b3b92e5f268e14b21ec6c42e9838ea1c578a11255635002`。

使用独立 `pr-native.html` 测试文档，包含 BOM、CRLF、中文、Emoji、pre、raw-text、独立 CSS 变换和更新状态文字的本地脚本；不使用用户文档。

| 项目 | 状态 |
| --- | --- |
| 真实键盘替换、退格、Enter/Esc、撤销/重做 | 通过：Keyboard-123 退格提交为 Keyboard-12；取消草稿，撤销后恢复原文，再重做 |
| 空片段跨动态预览重编 | 通过：清空块级标题及零宽行内 span，预览 → 编辑后均可重编，相邻 + 不变 |
| pre 首 LF 的动态预览与快照往返 | 通过：两个首 LF、中文、实体字符和 Emoji 保持一致，返回编辑后再次进入输入层核对值 |
| 变换及 raw-text 只读 | 通过：祖先 scale、元素 rotate 拒绝输入，xmp 保持只读 |
| 原生导出及重新打开 | 通过：导出与仅替换三个源区间的预期字节完全一致；BOM、CRLF、原脚本、相邻文字及原文件不变；重开后两个首 LF 与脚本状态均正确 |

原文件 SHA-256：`d1e49b705cc1278fd29f5fd124a0042e198cee7cd9a2a17a69073bbb4081f719`；716 字节导出 SHA-256：`cd96c57b52bac566f2b2a98dec4310c980b3b0fc3bdf51813485149afc918a26`。校验结果保存在忽略目录的 `pr-native-export-result.json`；最终 check/build/Clippy 日志使用 `pr-` 前缀。

## 原始基线的补充验收记录

以下为 9af26e5 基线上的修复构建证据，保留失败恢复及关闭流程的检查细节；它们不替代上述 PR 最终构建实测。

新构建应用路径：`src-tauri/target/release/bundle/macos/PageIn.app`，已通过进程路径确认运行的是此产物。可执行文件 SHA-256：`6ddbdf8b4fdd2ffc4ecf5fdd6b7d3300586715fe0db08d78fba7cdff6e232ddf`。使用独立测试 HTML，包含 BOM、CRLF、中文、Emoji、空片段、raw-text、独立 CSS 变换和原始脚本；不使用用户文档。

| 项目                           | 状态                                                                                                  |
| ------------------------------ | ----------------------------------------------------------------------------------------------------- |
| 真实键盘替换、退格、Enter/Esc  | 通过：键盘替换为 Keyboard-123，退格后提交为 Keyboard-12；Esc 恢复确认值                               |
| 清空后重编及撤销/重做          | 通过：空块级元素重编；零宽行内元素通过最小区域重编，邻接的 + 未被改写；撤销/重做正确                  |
| 失败后保留输入、修改重试       | 通过前端拒绝路径：粘贴 1 MiB + 1 字节、Enter 后提示超限，草稿与焦点保留，改为 Recovered-12 后提交成功 |
| 元素/祖先独立变换保持只读      | 通过：祖先 scale 与元素 rotate 显示不支持提示，未创建输入层；xmp 显示纯文本只读提示                   |
| 原生导出、重开及补丁外字节保真 | 通过：两份实际导出与预期字节完全一致；原文件、BOM、CRLF、原脚本及相邻文字保持不变；重开首换行正确     |
| 关闭时处理未确认输入           | 通过：输入中点关闭，完成提交后出现未导出确认；取消关闭后撤销，恢复已导出状态                          |
| 真实 IME 候选及最终 input      | 待验收：尝试原生输入源快捷键与逐键拼音，未取得候选状态；系统输入源 UI 自动化超时。粘贴不算 IME 验收   |

原生拒绝测试覆盖的是前端 UTF-8 长度预检；Rust IPC 拒绝、重试及取消已由生产事务/控制器的注入失败测试覆盖，未通过修改生产代码人为注入原生故障。1 MiB 超长单行测试期间，无障碍读取曾超时，后续快照确认界面恢复且修改已成功提交；本轮不作超大输入交互延迟的验收承诺。

两份导出检查脚本为 `artifacts/fix-2026-09-26/verify-native.mjs`，结果为 `native-export-result.json`。空片段样本同时覆盖有布局高度的空块级元素，以及宽度归零、紧邻另一个文本节点的 span；仅改变验收样本，不给产品加入占位节点或特例。

Windows 与 Omarchy 未在本轮实机验证。真实 IME 候选输入仍待验收，自动组合期测试不等于原生 IME 通过。多首 LF 等无法可靠映射的原始片段继续只读；plaintext 的既有预览序列化显示限制不在本次重构范围，其只读和原始导出字节已验证。
