# HTML 演示项目验收

日期：2026-09-18，macOS / WKWebView release。

## 自动检查

- TypeScript、19 项前端测试、9 项 Rust 测试通过；Clippy 无警告，macOS release 打包成功。
- 用户入口文件解析得到 294 个可编辑片段，序列化后重新解析，源路径映射失败数为 0。
- 增加脚本移除后的相邻文本合并回归用例，防止整篇文档的源路径偏移；跨删除标签的合并文本保持只读。
- 导出测试覆盖空目录、二进制文件、其他 HTML、原文件保留、已有目录保护、外部入口修改和符号链接拒绝。

## 实际项目

使用用户指定的 oss-strategy-ppt 目录：17 页、24 个文件。

- 原生文件夹选择器打开项目，封面、第二页、第三页和末页可见，第一页/末页的边界按钮正确禁用。
- 方向键、Home / End、底部分页按钮和滚轮翻页成功，编辑模式也能翻页。
- 封面第二行「开源策略对比」改为「开源策略验证」；第二页第一行「开引擎。」改为「开源引擎。」。分别验证键入英文、粘贴中文、Enter 提交，以及输入中直接点击整体导出。
- 整体导出到 artifacts/oss-strategy-ppt-verified：文件清单 24 / 24，其他 23 个文件 SHA-256 与原目录一致，空 images 目录保留。
- 导出的 index.html 与原文件的差异严格等于上述两处文字替换；br、所有脚本、样式和未编辑内容完全保留。原目录全部 24 个文件哈希未变。
- 撤销第二页改动后状态变为未导出；重做后恢复已导出状态。重新打开导出目录，17 页和修改后的标题正常显示。
- 最终应用已重新加载最新构建，恢复用户原项目封面，无测试文字留在当前会话。

## 边界

当前预览是静态分页，未运行页面的动画、WebGL 和远程资源；导出保留原有代码。没有将静态分页验收等同于所有演示框架兼容性验证。Windows / Linux 尚未实机测试。

原生目录选择器一次出现 Open 禁用，返回上级目录再进入后恢复。增加保存面板默认目录初始化时出现 Save 持续禁用，因此已撤回该初始化。重建并重启最终版本后，目录选择和 Save 按钮均正常；再次整体导出到 artifacts/oss-strategy-ppt-final-check，全部 24 个文件的哈希与原目录一致。最后再次核对原目录全部文件，均未改变。
